#include <iostream>
#include <string>

using namespace std;

int main(){
	setlocale(LC_ALL,"Russian");
	string b = "ab*cd";
	cout<<"Вести строку:";
//	cin>>b;
	
	string itog="";
	
	for (int i=0; i<b.length(); i++){
		if(b[i]!='*'){
			itog = itog+b[i]+b[i];
		}
	}
	cout<<"Преобразовать строку:"<<b<<endl;
	cout<<"Преобразовать строку:"<<itog<<endl;
	return 0;
}
